import { Base64ConverterPipe } from './base64-converter.pipe';

describe('Base64ConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new Base64ConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
